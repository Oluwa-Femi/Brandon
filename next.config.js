/** @type {import('next').NextConfig} */
const nextConfig = {
  customFonts: {
    clashDisplay: {
      files: ["fonts/clash-display/clash-display-*.otf"],
    },
  },
};

module.exports = nextConfig;
